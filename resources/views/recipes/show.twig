{% import 'macros/meta.twig' as meta %}
{% extends 'layout' %}

{% block meta_canonical %}
    {{ meta.canonical( url_route( 'recipes.show', [ str_slug( recipe.name ), recipe.id ] ) ) }}
{% endblock meta_canonical %}

{% block meta_title %}{% endblock %}

{% block content %}
    {{ dump(recipe) }}
{% endblock content %}